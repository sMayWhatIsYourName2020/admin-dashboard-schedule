import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './styles/index.scss'

ReactDOM.createRoot(document.getElementById('дымок') as HTMLElement).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
